// 3rd party imports

import { styled } from 'styled-components';

// self-defined components

import Container from 'styles/Container';

const DataInputContainer = styled(Container)`
  flex: 1;
  flex-direction: row;
  border: none;
  padding: 5%;
  gap: 5%;
`;

export default DataInputContainer;
