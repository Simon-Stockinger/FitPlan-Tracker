import styled from 'styled-components';
import { Text } from 'react-native';

const SetupScreen = () => {
  return <WelcomeHeadline>Setup Screen</WelcomeHeadline>;
};

const WelcomeHeadline = styled(Text)`
  font-size: 2rem;
`;

export default SetupScreen;
