// 3rd party imports

import { styled } from 'styled-components';
import WhiteText from './WhiteText';

const Headline = styled(WhiteText)`
  font-size: 24px;
  font-weight: bold;
  padding: 5%;
`;

export default Headline;
